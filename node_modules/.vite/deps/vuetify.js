import { computed, effectScope, nextTick, onBeforeUnmount, reactive, toValue, watch } from "./vue.runtime.esm-bundler-D72dexdH.js";
import { DefaultsSymbol, IN_BROWSER, consoleWarn, createDefaults, defineComponent, getCurrentInstance, isObject, mergeDeep, propsFactory, useDefaults } from "./defineComponent-Dz-_BdZg.js";
import "./proxiedModel-pSL3svcX.js";
import { ThemeSymbol, createTheme, useTheme } from "./theme-h65ayrCH.js";
import "./easing-Dv_s-_U7.js";
import { LocaleSymbol, createLocale, useLocale, useRtl } from "./locale-CFpsWEx1.js";
import { DateAdapterSymbol, DateOptionsSymbol, createDate, useDate } from "./date-CioiW1Bh.js";
import { DisplaySymbol, createDisplay, useDisplay } from "./display-ByTS_iV9.js";
import { GoToSymbol, createGoTo, useGoTo } from "./goto-D5eciX7X.js";
import { IconSymbol, createIcons } from "./icons-Cvqa-YK6.js";
import "./resizeObserver-D97-Sro_.js";
import { useLayout } from "./layout-dDSN9R_8.js";

//#region node_modules/vuetify/lib/composables/hotkey/key-aliases.js
/**
* Centralized key alias mapping for consistent key normalization across the hotkey system.
*
* This maps various user-friendly aliases to canonical key names that match
* KeyboardEvent.key values (in lowercase) where possible.
*/
const keyAliasMap = {
	control: "ctrl",
	command: "cmd",
	option: "alt",
	up: "arrowup",
	down: "arrowdown",
	left: "arrowleft",
	right: "arrowright",
	esc: "escape",
	spacebar: " ",
	space: " ",
	return: "enter",
	del: "delete",
	minus: "-",
	hyphen: "-"
};
/**
* Normalizes a key string to its canonical form using the alias map.
*
* @param key - The key string to normalize
* @returns The canonical key name in lowercase
*/
function normalizeKey(key) {
	const lowerKey = key.toLowerCase();
	return keyAliasMap[lowerKey] || lowerKey;
}

//#endregion
//#region node_modules/vuetify/lib/composables/hotkey/hotkey-parsing.js
/**
* Splits a single combination string into individual key parts.
*
* A combination is a set of keys that must be pressed simultaneously.
* e.g. `ctrl+k`, `shift--`
*/
function splitKeyCombination(combination) {
	let isInternal = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
	if (!combination) {
		if (!isInternal) consoleWarn("Invalid hotkey combination: empty string provided");
		return [];
	}
	const hasInvalidLeadingSeparator = (combination.startsWith("+") || combination.startsWith("_")) && !(combination.startsWith("++") || combination.startsWith("__"));
	if (combination.length > 1 && hasInvalidLeadingSeparator || combination.includes("++") || combination.includes("__") || combination === "+" || combination === "_" || combination.length > 1 && (combination.endsWith("+") || combination.endsWith("_")) && combination.at(-2) !== combination.at(-1) || combination === "++" || combination === "--" || combination === "__") {
		if (!isInternal) consoleWarn(`Invalid hotkey combination: "${combination}" has invalid structure`);
		return [];
	}
	const keys = [];
	let buffer = "";
	const flushBuffer = () => {
		if (buffer) {
			keys.push(normalizeKey(buffer));
			buffer = "";
		}
	};
	for (let i = 0; i < combination.length; i++) {
		const char = combination[i];
		const nextChar = combination[i + 1];
		if (char === "+" || char === "_" || char === "-") if (char === nextChar) {
			flushBuffer();
			keys.push(char);
			i++;
		} else if (char === "+" || char === "_") flushBuffer();
		else buffer += char;
		else buffer += char;
	}
	flushBuffer();
	if (keys.some((key) => key.length > 1 && key.includes("-") && key !== "--")) {
		if (!isInternal) consoleWarn(`Invalid hotkey combination: "${combination}" has invalid structure`);
		return [];
	}
	if (keys.length === 0 && combination) return [normalizeKey(combination)];
	return keys;
}
/**
* Splits a hotkey string into its constituent combination groups.
*
* A sequence is a series of combinations that must be pressed in order.
* e.g. `a-b`, `ctrl+k-p`
*/
function splitKeySequence(str) {
	if (!str) {
		consoleWarn("Invalid hotkey sequence: empty string provided");
		return [];
	}
	const hasInvalidStart = str.startsWith("-") && !["---", "--+"].includes(str);
	const hasInvalidEnd = str.endsWith("-") && !str.endsWith("+-") && !str.endsWith("_-") && str !== "-" && str !== "---";
	if (hasInvalidStart || hasInvalidEnd) {
		consoleWarn(`Invalid hotkey sequence: "${str}" contains invalid combinations`);
		return [];
	}
	const result = [];
	let buffer = "";
	let i = 0;
	while (i < str.length) {
		const char = str[i];
		if (char === "-") {
			const prevChar = str[i - 1];
			const prevPrevChar = i > 1 ? str[i - 2] : void 0;
			if ((prevChar === "+" || prevChar === "_") && prevPrevChar !== "+") {
				buffer += char;
				i++;
			} else {
				if (buffer) {
					result.push(buffer);
					buffer = "";
				} else result.push("-");
				i++;
			}
		} else {
			buffer += char;
			i++;
		}
	}
	if (buffer) result.push(buffer);
	const collapsed = [];
	let minusCount = 0;
	for (const part of result) if (part === "-") {
		if (minusCount % 2 === 0) collapsed.push("-");
		minusCount++;
	} else {
		minusCount = 0;
		collapsed.push(part);
	}
	if (!collapsed.every((s) => splitKeyCombination(s, true).length > 0)) {
		consoleWarn(`Invalid hotkey sequence: "${str}" contains invalid combinations`);
		return [];
	}
	return collapsed;
}

//#endregion
//#region node_modules/vuetify/lib/composables/hotkey/hotkey.js
function useHotkey(keys, callback) {
	let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
	if (!IN_BROWSER) return function() {};
	const { event = "keydown", inputs = false, preventDefault = true, sequenceTimeout = 1e3 } = options;
	const isMac = navigator?.userAgent?.includes("Macintosh") ?? false;
	let timeout = 0;
	let keyGroups;
	let isSequence = false;
	let groupIndex = 0;
	function clearTimer() {
		if (!timeout) return;
		clearTimeout(timeout);
		timeout = 0;
	}
	function isInputFocused() {
		if (toValue(inputs)) return false;
		const activeElement = document.activeElement;
		return activeElement && (activeElement.tagName === "INPUT" || activeElement.tagName === "TEXTAREA" || activeElement.isContentEditable || activeElement.contentEditable === "true");
	}
	function resetSequence() {
		groupIndex = 0;
		clearTimer();
	}
	function handler(e) {
		const group = keyGroups[groupIndex];
		if (!group || isInputFocused()) return;
		if (!matchesKeyGroup(e, group)) {
			if (isSequence) resetSequence();
			return;
		}
		if (toValue(preventDefault)) e.preventDefault();
		if (!isSequence) {
			callback(e);
			return;
		}
		clearTimer();
		groupIndex++;
		if (groupIndex === keyGroups.length) {
			callback(e);
			resetSequence();
			return;
		}
		timeout = window.setTimeout(resetSequence, toValue(sequenceTimeout));
	}
	function cleanup() {
		window.removeEventListener(toValue(event), handler);
		clearTimer();
	}
	watch(() => toValue(keys), function(unrefKeys) {
		cleanup();
		if (unrefKeys) {
			const groups = splitKeySequence(unrefKeys.toLowerCase());
			isSequence = groups.length > 1;
			keyGroups = groups;
			resetSequence();
			window.addEventListener(toValue(event), handler);
		}
	}, { immediate: true });
	watch(() => toValue(event), function(newEvent, oldEvent) {
		if (oldEvent && keyGroups && keyGroups.length > 0) {
			window.removeEventListener(oldEvent, handler);
			window.addEventListener(newEvent, handler);
		}
	});
	try {
		getCurrentInstance("useHotkey");
		onBeforeUnmount(cleanup);
	} catch {}
	function parseKeyGroup(group) {
		const MODIFIERS = [
			"ctrl",
			"shift",
			"alt",
			"meta",
			"cmd"
		];
		const parts = splitKeyCombination(group.toLowerCase());
		if (parts.length === 0) return {
			modifiers: Object.fromEntries(MODIFIERS.map((m) => [m, false])),
			actualKey: void 0
		};
		const modifiers = Object.fromEntries(MODIFIERS.map((m) => [m, false]));
		let actualKey;
		for (const part of parts) if (MODIFIERS.includes(part)) modifiers[part] = true;
		else actualKey = part;
		return {
			modifiers,
			actualKey
		};
	}
	function matchesKeyGroup(e, group) {
		const { modifiers, actualKey } = parseKeyGroup(group);
		const expectCtrl = modifiers.ctrl || !isMac && (modifiers.cmd || modifiers.meta);
		const expectMeta = isMac && (modifiers.cmd || modifiers.meta);
		return e.ctrlKey === expectCtrl && e.metaKey === expectMeta && e.shiftKey === modifiers.shift && e.altKey === modifiers.alt && e.key.toLowerCase() === actualKey?.toLowerCase();
	}
	return cleanup;
}

//#endregion
//#region node_modules/vuetify/lib/composables/mask/mask.js
const makeMaskProps = propsFactory({ mask: [String, Object] }, "mask");
const defaultDelimiters = /[-!$%^&*()_+|~=`{}[\]:";'<>?,./\\ ]/;
var presets = {
	"credit-card": "#### - #### - #### - ####",
	date: "##/##/####",
	"date-time": "##/##/#### ##:##",
	"iso-date": "####-##-##",
	"iso-date-time": "####-##-## ##:##",
	phone: "(###) ### - ####",
	social: "###-##-####",
	time: "##:##",
	"time-with-seconds": "##:##:##"
};
var defaultTokens = {
	"#": { pattern: /[0-9]/ },
	A: {
		pattern: /[A-Z]/i,
		convert: (v) => v.toUpperCase()
	},
	a: {
		pattern: /[a-z]/i,
		convert: (v) => v.toLowerCase()
	},
	N: {
		pattern: /[0-9A-Z]/i,
		convert: (v) => v.toUpperCase()
	},
	n: {
		pattern: /[0-9a-z]/i,
		convert: (v) => v.toLowerCase()
	},
	X: { pattern: defaultDelimiters }
};
function useMask(props) {
	const mask = computed(() => {
		if (typeof props.mask === "string") {
			if (props.mask in presets) return presets[props.mask];
			return props.mask;
		}
		return props.mask?.mask ?? "";
	});
	const tokens = computed(() => {
		return {
			...defaultTokens,
			...isObject(props.mask) ? props.mask.tokens : null
		};
	});
	function isMask(char) {
		return char in tokens.value;
	}
	function maskValidates(mask$1, char) {
		if (char == null || !isMask(mask$1)) return false;
		const item = tokens.value[mask$1];
		if (item.pattern) return item.pattern.test(char);
		return item.test(char);
	}
	function convert(mask$1, char) {
		const item = tokens.value[mask$1];
		return item.convert ? item.convert(char) : char;
	}
	function maskText(text) {
		const trimmedText = text?.trim().replace(/\s+/g, " ");
		if (trimmedText == null) return "";
		if (!mask.value.length || !trimmedText.length) return trimmedText;
		let textIndex = 0;
		let maskIndex = 0;
		let newText = "";
		while (maskIndex < mask.value.length) {
			const mchar = mask.value[maskIndex];
			const tchar = trimmedText[textIndex];
			if (mchar === "\\") {
				newText += mask.value[maskIndex + 1];
				maskIndex += 2;
				continue;
			}
			if (!isMask(mchar)) {
				newText += mchar;
				if (tchar === mchar) textIndex++;
			} else if (maskValidates(mchar, tchar)) {
				newText += convert(mchar, tchar);
				textIndex++;
			} else break;
			maskIndex++;
		}
		return newText;
	}
	function unmaskText(text) {
		if (text == null) return null;
		if (!mask.value.length || !text.length) return text;
		let textIndex = 0;
		let maskIndex = 0;
		let newText = "";
		while (true) {
			const mchar = mask.value[maskIndex];
			const tchar = text[textIndex];
			if (tchar == null) break;
			if (mchar == null) {
				newText += tchar;
				textIndex++;
				continue;
			}
			if (mchar === "\\") {
				if (tchar === mask.value[maskIndex + 1]) textIndex++;
				maskIndex += 2;
				continue;
			}
			if (maskValidates(mchar, tchar)) {
				newText += tchar;
				textIndex++;
				maskIndex++;
				continue;
			} else if (mchar !== tchar) {
				while (true) {
					const mchar$1 = mask.value[maskIndex++];
					if (mchar$1 == null || maskValidates(mchar$1, tchar)) break;
				}
				continue;
			}
			textIndex++;
			maskIndex++;
		}
		return newText;
	}
	function isValid(text) {
		if (!text) return false;
		return unmaskText(text) === unmaskText(maskText(text));
	}
	function isComplete(text) {
		if (!text) return false;
		return maskText(text).length === mask.value.length && isValid(text);
	}
	return {
		isValid,
		isComplete,
		mask: maskText,
		unmask: unmaskText
	};
}

//#endregion
//#region node_modules/vuetify/lib/framework.js
function createVuetify() {
	const { blueprint,...rest } = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
	const options = mergeDeep(blueprint, rest);
	const { aliases = {}, components = {}, directives = {} } = options;
	const scope = effectScope();
	return scope.run(() => {
		const defaults = createDefaults(options.defaults);
		const display = createDisplay(options.display, options.ssr);
		const theme = createTheme(options.theme);
		const icons = createIcons(options.icons);
		const locale = createLocale(options.locale);
		const date = createDate(options.date, locale);
		const goTo = createGoTo(options.goTo, locale);
		function install(app) {
			for (const key in directives) app.directive(key, directives[key]);
			for (const key in components) app.component(key, components[key]);
			for (const key in aliases) app.component(key, defineComponent({
				...aliases[key],
				name: key,
				aliasName: aliases[key].name
			}));
			const appScope = effectScope();
			appScope.run(() => {
				theme.install(app);
			});
			app.onUnmount(() => appScope.stop());
			app.provide(DefaultsSymbol, defaults);
			app.provide(DisplaySymbol, display);
			app.provide(ThemeSymbol, theme);
			app.provide(IconSymbol, icons);
			app.provide(LocaleSymbol, locale);
			app.provide(DateOptionsSymbol, date.options);
			app.provide(DateAdapterSymbol, date.instance);
			app.provide(GoToSymbol, goTo);
			if (IN_BROWSER && options.ssr) if (app.$nuxt) app.$nuxt.hook("app:suspense:resolve", () => {
				display.update();
			});
			else {
				const { mount } = app;
				app.mount = function() {
					const vm = mount(...arguments);
					nextTick(() => display.update());
					app.mount = mount;
					return vm;
				};
			}
			if (typeof __VUE_OPTIONS_API__ !== "boolean" || __VUE_OPTIONS_API__) app.mixin({ computed: { $vuetify() {
				return reactive({
					defaults: inject.call(this, DefaultsSymbol),
					display: inject.call(this, DisplaySymbol),
					theme: inject.call(this, ThemeSymbol),
					icons: inject.call(this, IconSymbol),
					locale: inject.call(this, LocaleSymbol),
					date: inject.call(this, DateAdapterSymbol)
				});
			} } });
		}
		function unmount() {
			scope.stop();
		}
		return {
			install,
			unmount,
			defaults,
			display,
			theme,
			icons,
			locale,
			date,
			goTo
		};
	});
}
const version = "3.10.3";
createVuetify.version = version;
function inject(key) {
	const vm = this.$;
	const provides = vm.parent?.provides ?? vm.vnode.appContext?.provides;
	if (provides && key in provides) return provides[key];
}

//#endregion
export { createVuetify, useDate, useDefaults, useDisplay, useGoTo, useHotkey, useLayout, useLocale, useMask, useRtl, useTheme, version };
//# sourceMappingURL=vuetify.js.map