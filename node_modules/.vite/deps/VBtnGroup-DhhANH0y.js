import { createVNode, normalizeClass, normalizeStyle, toRef } from "./vue.runtime.esm-bundler-D72dexdH.js";
import { genericComponent, propsFactory, provideDefaults } from "./defineComponent-Dz-_BdZg.js";
import { makeComponentProps, useRender } from "./useRender-C-FT7xyL.js";
import { makeThemeProps, provideTheme } from "./theme-h65ayrCH.js";
import { makeTagProps } from "./tag-vlkYG7tY.js";
import { makeRoundedProps, useRounded } from "./rounded-vOX0OjBM.js";
import { makeBorderProps, useBorder } from "./border-HL-gLqdp.js";
import { makeElevationProps, useElevation } from "./elevation-BHzehScM.js";
import { makeDensityProps, useDensity } from "./density-BtO4U0lf.js";
import { makeVariantProps } from "./variant-2-GSSw3U.js";
import "/home/maryam/nadinSoft/project/node_modules/vuetify/lib/components/VBtnGroup/VBtnGroup.css";

//#region node_modules/vuetify/lib/components/VBtnGroup/VBtnGroup.js
const makeVBtnGroupProps = propsFactory({
	baseColor: String,
	divided: Boolean,
	direction: {
		type: String,
		default: "horizontal"
	},
	...makeBorderProps(),
	...makeComponentProps(),
	...makeDensityProps(),
	...makeElevationProps(),
	...makeRoundedProps(),
	...makeTagProps(),
	...makeThemeProps(),
	...makeVariantProps()
}, "VBtnGroup");
const VBtnGroup = genericComponent()({
	name: "VBtnGroup",
	props: makeVBtnGroupProps(),
	setup(props, _ref) {
		let { slots } = _ref;
		const { themeClasses } = provideTheme(props);
		const { densityClasses } = useDensity(props);
		const { borderClasses } = useBorder(props);
		const { elevationClasses } = useElevation(props);
		const { roundedClasses } = useRounded(props);
		provideDefaults({ VBtn: {
			height: toRef(() => props.direction === "horizontal" ? "auto" : null),
			baseColor: toRef(() => props.baseColor),
			color: toRef(() => props.color),
			density: toRef(() => props.density),
			flat: true,
			variant: toRef(() => props.variant)
		} });
		useRender(() => {
			return createVNode(props.tag, {
				"class": normalizeClass([
					"v-btn-group",
					`v-btn-group--${props.direction}`,
					{ "v-btn-group--divided": props.divided },
					themeClasses.value,
					borderClasses.value,
					densityClasses.value,
					elevationClasses.value,
					roundedClasses.value,
					props.class
				]),
				"style": normalizeStyle(props.style)
			}, slots);
		});
	}
});

//#endregion
export { VBtnGroup, makeVBtnGroupProps };
//# sourceMappingURL=VBtnGroup-DhhANH0y.js.map