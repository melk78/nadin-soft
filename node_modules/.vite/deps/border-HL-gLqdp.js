import { computed } from "./vue.runtime.esm-bundler-D72dexdH.js";
import { getCurrentInstanceName, propsFactory } from "./defineComponent-Dz-_BdZg.js";

//#region node_modules/vuetify/lib/composables/border.js
const makeBorderProps = propsFactory({ border: [
	Boolean,
	Number,
	String
] }, "border");
function useBorder(props) {
	let name = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : getCurrentInstanceName();
	return { borderClasses: computed(() => {
		const border = props.border;
		if (border === true || border === "") return `${name}--border`;
		else if (typeof border === "string" || border === 0) return String(border).split(" ").map((v) => `border-${v}`);
		return [];
	}) };
}

//#endregion
export { makeBorderProps, useBorder };
//# sourceMappingURL=border-HL-gLqdp.js.map